<Window x:Class="TourPlanner.PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.PL"
        xmlns:view="clr-namespace:TourPlanner.PL.View"
        xmlns:viewmodel="clr-namespace:TourPlanner.Client.BL.ViewModel;assembly=TourPlanner.Client.BL" 
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="580" 
        Width="930"
        >
    <DockPanel>
        <!--<view:HomeView DockPanel.Dock="Top" DataContext="{Binding HomeVM}"/>-->
        <!--<view:AddTourView DockPanel.Dock="Top" DataContext="{Binding AddTourVM}"/>-->
        <!--<view:AddTourLogView DockPanel.Dock="Top" DataContext="{Binding AddTourLogVM}"/>-->
        <ContentControl Content="{Binding SelectedViewModel}"/>
    </DockPanel>
</Window>
